---
phase: 06-polish-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/bom/AddItemForm.svelte
  - src/lib/components/bom/BOMDisplay.svelte
  - src/routes/+layout.svelte
autonomous: true

must_haves:
  truths:
    - "AddItemForm fields stack vertically on mobile (<640px)"
    - "AddItemForm fields display in row on tablet/desktop (>=640px)"
    - "BOM header buttons wrap to new line on narrow screens"
    - "Layout padding is smaller on mobile (px-4), larger on desktop (px-6)"
  artifacts:
    - path: "src/lib/components/bom/AddItemForm.svelte"
      provides: "Responsive form layout"
      contains: "sm:flex-row|sm:flex"
    - path: "src/lib/components/bom/BOMDisplay.svelte"
      provides: "Responsive header buttons"
      contains: "flex-wrap|sm:flex-nowrap"
    - path: "src/routes/+layout.svelte"
      provides: "Responsive padding"
      contains: "px-4 sm:px-6"
  key_links:
    - from: "AddItemForm"
      to: "Tailwind breakpoints"
      via: "sm: prefix classes"
      pattern: "sm:(flex-row|w-)"
---

<objective>
Fix responsive layout issues so the BOM tool is usable on mobile and tablet devices.

Purpose: Current layout breaks on narrow screens - AddItemForm fields overflow and buttons don't wrap.
Output: Properly responsive components that work from 320px to desktop widths
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/components/bom/AddItemForm.svelte
@src/lib/components/bom/BOMDisplay.svelte
@src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AddItemForm and BOMDisplay responsive layouts</name>
  <files>src/lib/components/bom/AddItemForm.svelte, src/lib/components/bom/BOMDisplay.svelte</files>
  <action>
  **AddItemForm.svelte** - Change from single row to stacked mobile layout:

  Current form element:
  ```svelte
  <form
    onsubmit={handleSubmit}
    class="flex flex-wrap items-end gap-2 border-t border-gray-200 bg-gray-50 p-3"
  >
  ```

  Change to stacking layout:
  ```svelte
  <form
    onsubmit={handleSubmit}
    class="flex flex-col gap-3 border-t border-gray-200 bg-gray-50 p-3 sm:flex-row sm:flex-wrap sm:items-end sm:gap-2"
  >
  ```

  Update field containers:
  1. Name field: Change `class="min-w-[150px] flex-1"` to `class="w-full sm:min-w-[150px] sm:flex-1"`
  2. Qty field: Change `class="w-20"` to `class="w-full sm:w-20"`
  3. Unit field: Change `class="w-24"` to `class="w-full sm:w-24"`
  4. Notes field: Change `class="min-w-[100px] flex-1"` to `class="w-full sm:min-w-[100px] sm:flex-1"`
  5. Button container: Change `class="flex gap-2"` to `class="flex gap-2 sm:flex-none"`

  **BOMDisplay.svelte** - Make header buttons wrap:

  Current header div (around line 69):
  ```svelte
  <div class="mb-6 flex items-start justify-between">
  ```

  Change to allow wrapping:
  ```svelte
  <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
  ```

  Button container (around line 78):
  ```svelte
  <div class="flex gap-2">
  ```

  Change to:
  ```svelte
  <div class="flex flex-wrap gap-2">
  ```
  </action>
  <verify>
  Read both updated files and confirm:
  - AddItemForm has `flex-col` base with `sm:flex-row`
  - AddItemForm fields have `w-full` base with `sm:` overrides
  - BOMDisplay header has `flex-col` base with `sm:flex-row`
  - BOMDisplay buttons have `flex-wrap`
  </verify>
  <done>AddItemForm stacks on mobile, shows row on desktop. BOMDisplay header and buttons wrap properly on narrow screens.</done>
</task>

<task type="auto">
  <name>Task 2: Make layout padding responsive</name>
  <files>src/routes/+layout.svelte</files>
  <action>
  Current main element (line 15):
  ```svelte
  <main class="max-w-6xl mx-auto px-6 py-8">
  ```

  Change to responsive padding:
  ```svelte
  <main class="max-w-6xl mx-auto px-4 py-6 sm:px-6 sm:py-8">
  ```

  This gives:
  - Mobile (<640px): 16px horizontal padding, 24px vertical padding
  - Tablet/Desktop (>=640px): 24px horizontal padding, 32px vertical padding
  </action>
  <verify>
  Read updated file and confirm `px-4 py-6 sm:px-6 sm:py-8` classes are present
  </verify>
  <done>Layout has smaller padding on mobile, larger on desktop</done>
</task>

</tasks>

<verification>
1. Code review: Verify responsive Tailwind classes are correct
2. Manual test: View on narrow viewport (use browser dev tools at 375px width)
</verification>

<success_criteria>
- AddItemForm fields stack on mobile, row on desktop
- BOMDisplay header wraps properly
- Layout padding adjusts by breakpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-integration/06-02-SUMMARY.md`
</output>
