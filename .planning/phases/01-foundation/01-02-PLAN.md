---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - src/lib/components/Header.svelte
  - src/lib/components/ToolCard.svelte
autonomous: false
must_haves:
  truths:
    - "User can visit localhost:5173 and see dashboard"
    - "Dashboard shows BOM Generator tool card"
    - "Header with app branding is visible"
    - "Layout provides consistent shell across pages"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "App shell with header and slot"
      min_lines: 15
    - path: "src/routes/+page.svelte"
      provides: "Dashboard home page"
      min_lines: 20
    - path: "src/lib/components/Header.svelte"
      provides: "Header with app name"
      min_lines: 10
    - path: "src/lib/components/ToolCard.svelte"
      provides: "Reusable tool card component"
      min_lines: 20
  key_links:
    - from: "+layout.svelte"
      to: "Header.svelte"
      via: "import and render"
      pattern: "import.*Header"
    - from: "+page.svelte"
      to: "ToolCard.svelte"
      via: "import and render"
      pattern: "import.*ToolCard"
---

<objective>
Create dashboard UI with app shell layout.

Purpose: Build the visual foundation - header, layout, and dashboard with tool cards.
Output: Working dashboard page that displays the BOM Generator tool card placeholder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Design context from PROJECT.md:
- Dashboard home page with tool cards for navigation
- Modular tool architecture (BOM Generator is first tool)
- Responsive design for desktop and tablet

Color/styling guidance: Use Tailwind defaults initially. Simple, clean, functional.
The app is "WoodShop Toolbox" - a personal woodworking tools platform.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component</name>
  <files>src/lib/components/Header.svelte</files>
  <action>
    Create src/lib/components/Header.svelte:

    - Simple header with app name "WoodShop Toolbox"
    - Use semantic HTML: header element
    - Tailwind styling: fixed or sticky top, background color, padding
    - Include a simple logo placeholder (text-based or emoji like 🪵 or 🔨)
    - Keep it minimal - no navigation yet (future feature)

    Example structure:
    ```svelte
    <header class="bg-amber-800 text-white px-6 py-4 shadow-md">
      <div class="max-w-6xl mx-auto flex items-center gap-3">
        <span class="text-2xl">🪵</span>
        <h1 class="text-xl font-semibold">WoodShop Toolbox</h1>
      </div>
    </header>
    ```

    Use warm wood-toned colors (amber/brown palette) to match the woodworking theme.
  </action>
  <verify>Component has no TypeScript errors</verify>
  <done>Header component created with app branding</done>
</task>

<task type="auto">
  <name>Task 2: Create ToolCard component</name>
  <files>src/lib/components/ToolCard.svelte</files>
  <action>
    Create src/lib/components/ToolCard.svelte:

    Props (TypeScript):
    - title: string (tool name)
    - description: string (short description)
    - href: string (link to the tool)
    - icon: string (emoji or icon)
    - disabled?: boolean (for future tools not yet implemented)

    Structure:
    - Card container with hover effects
    - Icon/emoji display
    - Title and description
    - Visual indicator for disabled state (greyed out, "Coming Soon" badge)
    - Use anchor tag for navigation (href prop)

    Styling with Tailwind:
    - Rounded corners, shadow
    - Hover: lift effect (transform, shadow increase)
    - Disabled: opacity reduced, cursor-not-allowed

    Example:
    ```svelte
    <script lang="ts">
      export let title: string;
      export let description: string;
      export let href: string;
      export let icon: string = "🔧";
      export let disabled: boolean = false;
    </script>

    <a
      {href}
      class="block p-6 rounded-xl border bg-white shadow-sm hover:shadow-md transition-all {disabled ? 'opacity-50 cursor-not-allowed' : 'hover:-translate-y-1'}"
      class:pointer-events-none={disabled}
    >
      <div class="text-4xl mb-3">{icon}</div>
      <h2 class="text-lg font-semibold text-gray-900">{title}</h2>
      <p class="text-sm text-gray-600 mt-1">{description}</p>
      {#if disabled}
        <span class="inline-block mt-3 text-xs bg-gray-200 text-gray-600 px-2 py-1 rounded">Coming Soon</span>
      {/if}
    </a>
    ```
  </action>
  <verify>Component accepts all props, no TypeScript errors</verify>
  <done>ToolCard component created with proper TypeScript types</done>
</task>

<task type="auto">
  <name>Task 3: Create app layout</name>
  <files>src/routes/+layout.svelte</files>
  <action>
    Create/update src/routes/+layout.svelte:

    - Import Header component
    - Import app.css for Tailwind styles
    - Render Header at top
    - Main content area with slot
    - Basic page structure with max-width container

    Structure:
    ```svelte
    <script>
      import Header from '$lib/components/Header.svelte';
      import '../app.css';
    </script>

    <div class="min-h-screen bg-gray-50">
      <Header />
      <main class="max-w-6xl mx-auto px-6 py-8">
        <slot />
      </main>
    </div>
    ```

    Keep it simple - this is the shell that wraps all pages.
  </action>
  <verify>Layout renders without errors when running npm run dev</verify>
  <done>App layout created with Header and main content slot</done>
</task>

<task type="auto">
  <name>Task 4: Create dashboard page</name>
  <files>src/routes/+page.svelte</files>
  <action>
    Create/update src/routes/+page.svelte:

    - Import ToolCard component
    - Page heading: "Your Tools" or "Dashboard"
    - Grid of tool cards
    - BOM Generator card (primary tool, enabled, links to /bom - will 404 for now)
    - Optionally add 1-2 disabled "Coming Soon" cards to show the modular concept

    Structure:
    ```svelte
    <script>
      import ToolCard from '$lib/components/ToolCard.svelte';
    </script>

    <svelte:head>
      <title>WoodShop Toolbox</title>
    </svelte:head>

    <div>
      <h1 class="text-2xl font-bold text-gray-900 mb-6">Your Tools</h1>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <ToolCard
          title="BOM Generator"
          description="Generate comprehensive bills of materials for your woodworking projects with AI assistance."
          href="/bom"
          icon="📋"
        />

        <ToolCard
          title="Cut List Optimizer"
          description="Optimize your cuts to minimize waste and save material."
          href="/cutlist"
          icon="✂️"
          disabled={true}
        />

        <ToolCard
          title="Wood Movement Calculator"
          description="Calculate seasonal wood movement for your designs."
          href="/calculator"
          icon="📐"
          disabled={true}
        />
      </div>
    </div>
    ```

    The disabled tools show the platform's modular future. BOM Generator is the only active tool.
  </action>
  <verify>Dashboard renders with tool cards when visiting localhost:5173</verify>
  <done>Dashboard page created with BOM Generator tool card and placeholder future tools</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard with app shell (header, layout) and tool cards</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173
    3. Verify header shows "WoodShop Toolbox" with wood emoji
    4. Verify "Your Tools" heading is visible
    5. Verify BOM Generator card is visible and not greyed out
    6. Verify clicking BOM Generator card navigates (will 404 - that's expected)
    7. Verify Coming Soon tools are greyed out with badge
    8. Resize browser: cards should stack on mobile, show in grid on desktop
    9. Check: No visual glitches, clean layout
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` completes without errors
- [ ] Header displays with app name
- [ ] Dashboard shows tool cards
- [ ] Tool cards have working hover effects
- [ ] Responsive layout works (mobile/tablet/desktop)
- [ ] User approved visual verification
</verification>

<success_criteria>
- All tasks completed
- App shell layout in place (header, main content)
- Dashboard displays tool cards
- BOM Generator card is clickable (links to /bom)
- Responsive grid layout works
- User verified visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
