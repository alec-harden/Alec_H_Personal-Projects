# Phase 24 Plan 01: Display Updates

---
wave: 1
depends_on: []
files_modified:
  - src/lib/components/bom/BOMDisplay.svelte
  - src/lib/components/bom/BOMCategory.svelte
  - src/lib/components/bom/BOMItem.svelte
  - src/lib/components/bom/AddItemForm.svelte
  - src/lib/utils/csv.ts
autonomous: true
---

## Objective

Update BOM UI components to support the 6-category structure (hardwood, common, sheet, hardware, finishes, consumables) established in Phase 23.

## Requirements Addressed

- **UI-01**: Update category order and color configuration for 6 categories
- **UI-02**: Remove board feet calculations from BOMItem and BOMCategory components
- **UI-03**: Display lumber item names with fractional thickness prefix ("3/4 Oak")
- **UI-04**: Force `unit = 'pcs'` for all lumber categories in AddItemForm
- **UI-05**: Update category header labels (Hardwood Lumber, Common Boards, Sheet Goods)

## Tasks

<task id="1" title="Update BOMDisplay category order">
Update categoryOrder array from 4 to 6 categories in BOMDisplay.svelte.
</task>

<task id="2" title="Update BOMCategory config and labels">
Expand categoryConfig object in BOMCategory.svelte:
- Add entries for hardwood, common, sheet
- Update labels: "Hardwood Lumber", "Common Boards", "Sheet Goods"
- Assign distinct colors for each category
- Remove orphaned .board-feet-total CSS
</task>

<task id="3" title="Update BOMItem with thickness prefix">
In BOMItem.svelte:
- Import isLumberCategory from bom.ts
- Add thickness prefix to lumber item names (formatDimension(thickness) + name)
- Update lumber category check from `=== 'lumber'` to `isLumberCategory()`
- Remove orphaned .board-feet CSS
</task>

<task id="4" title="Update AddItemForm unit options">
Update unitOptions in AddItemForm.svelte:
- Expand to 6 categories
- Lumber categories (hardwood, common, sheet) only offer 'pcs'
</task>

<task id="5" title="Fix csv.ts type error">
Update categoryOrder in csv.ts to use new 6-category structure to fix type error.
</task>

## Verification

- [ ] BOM displays 6 categories with distinct colors
- [ ] Category labels show "Hardwood Lumber", "Common Boards", "Sheet Goods"
- [ ] Lumber items display thickness prefix ("3/4 Oak")
- [ ] AddItemForm only offers 'pcs' for lumber categories
- [ ] No board feet visible in UI
- [ ] TypeScript compilation passes
- [ ] Build succeeds

## must_haves

- 6-category display working
- Thickness prefix on lumber names
- Unit restriction for lumber categories
- No board feet artifacts in UI
